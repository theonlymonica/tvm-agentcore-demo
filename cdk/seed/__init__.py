"""
Documents seed generator package.

This package provides a parametric generator that produces the Documents_Table
seed data for the Session-Aware Document Guard demo. The generator builds
deterministic, scope-prefixed document items with stable IDs so that re-seeding
overwrites the same keys and never creates duplicates.
"""
